================================================================================
                    🤖 AGENT CHATBOT - PROJECT DOCUMENTATION
================================================================================

PROJECT OVERVIEW
================================================================================
A fully local, free AI chatbot that answers questions about agents and login 
data using Mistral 7B via Ollama. No cloud services, no API costs.

TECH STACK
- Frontend: React + Vite
- Backend: Node.js + Express
- Database: MongoDB (local)
- AI Model: Ollama (Mistral 7B)
- Cost: $0 (100% free)

================================================================================
                              PREREQUISITES
================================================================================

1. Node.js v18+ - https://nodejs.org/
2. MongoDB running on port 27017
3. Ollama installed - https://ollama.ai/

REQUIRED MONGODB COLLECTIONS:
- agents: UserName, Name, Nationality, Comp_Name, Date_establishment, 
          AgentID, CreatedDate, LastLogin
- agentlogins: AGENTID, ID, LOGINDATE

================================================================================
                              INSTALLATION
================================================================================

STEP 1: INSTALL OLLAMA & MISTRAL
---------------------------------
# Download from https://ollama.ai/
ollama pull mistral:7b
ollama run mistral:7b
# Type /bye to exit

STEP 2: START MONGODB
---------------------------------
mongod

STEP 3: SETUP BACKEND
---------------------------------
cd backend
npm install

# Edit .env if needed:
PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/testdb
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=mistral:7b

npm start

# Expected output:
✅ Connected to MongoDB: mongodb://127.0.0.1:27017/testdb
✅ Loaded 15 intents
🚀 Server running on http://localhost:5000

STEP 4: SETUP FRONTEND
---------------------------------
cd frontend
npm install
npm run dev

# Open http://localhost:3000

================================================================================
                          PROJECT STRUCTURE
================================================================================

chatbot-project/
├── backend/
│   ├── config/
│   │   ├── db.js                    # MongoDB connection
│   │   └── ollama.js                # Ollama configuration
│   ├── intents/
│   │   └── intents.json             # ⭐ All questions + queries
│   ├── routes/
│   │   └── chat.js                  # Main chat endpoint + sessions
│   ├── services/
│   │   ├── ollamaService.js         # Talks to Mistral
│   │   ├── dbService.js             # Runs MongoDB queries + pagination
│   │   └── intentService.js         # Matches intents
│   ├── utils/
│   │   ├── promptBuilder.js         # Extracts parameters + pagination
│   │   └── responseFormatter.js     # Formats results
│   ├── middleware/
│   │   ├── errorHandler.js
│   │   └── logger.js
│   ├── server.js                    # Entry point
│   ├── .env
│   └── package.json
│
└── frontend/
    ├── src/
    │   ├── components/
    │   │   ├── ChatWindow.jsx       # Main chat container
    │   │   ├── MessageBubble.jsx    # Individual messages
    │   │   ├── InputBar.jsx         # User input
    │   │   ├── DataTable.jsx        # Results table + pagination
    │   │   └── TypingIndicator.jsx  # Loading animation
    │   ├── hooks/
    │   │   └── useChat.js           # Chat state management
    │   ├── services/
    │   │   └── chatApi.js           # API calls
    │   ├── styles/
    │   │   ├── chat.css
    │   │   └── table.css            # Table + pagination styles
    │   ├── App.jsx
    │   └── main.jsx
    ├── index.html
    ├── .env
    └── package.json

================================================================================
                          HOW IT WORKS
================================================================================

FLOW DIAGRAM:
-------------
User: "Show agents from India"
        ↓
React sends to Node.js backend
        ↓
Backend extracts parameters (country: "India", page: 1, limit: 10)
        ↓
Backend sends to Ollama: "What intent is this?"
        ↓
Ollama returns: "get_agents_by_nationality"
        ↓
Backend finds matching query in intents.json
        ↓
Backend runs MongoDB query with pagination
        ↓
MongoDB returns: { data: [...], pagination: { page: 1, total: 47 } }
        ↓
Backend formats: "Showing 1-10 of 47 results."
        ↓
React displays answer + table with Prev/Next buttons

PAGINATION FLOW:
----------------
User clicks [Next ▶] or types "show next"
        ↓
Backend reads session (no Ollama call needed!)
        ↓
Increments page: 1 → 2
        ↓
Runs same query with skip(10).limit(10)
        ↓
Returns page 2 results (< 100ms)

================================================================================
                          SUPPORTED QUERIES
================================================================================

AGENT DETAILS
-------------
"Show me details of agent gautam@cultureholidays.com"
"Who is CHAGT0001000024104?"
"Find agent by name Ray Harper"
"What company does gautam work for?"

LOGIN ACTIVITY
--------------
"When did ashish.bhasin@travelchacha.com last login?"
"Show recent logins"
"Who logged in today?"
"Who logged in this week?"

COUNTS & STATS
--------------
"How many total agents are there?"
"How many agents are from India?"
"How many agents logged in today?"

COMPANY QUESTIONS
-----------------
"List all companies"
"Show agents of Culture Holidays"
"How many agents does LUXE GRAND TRAVEL have?"
"Which company was established first?"
"What is the oldest company?"
"Which company is newest?"

INACTIVE AGENTS
---------------
"Which agents have not logged in for 30 days?"
"Show inactive agents"
"Who hasn't logged in for 6 months?"

SEARCH
------
"Search agent by email luxegrandtravel@mail.com"
"Find agents with gmail"
"Find agent ID for Ray Harper"

PAGINATION
----------
"Show first 10 agents from India"
"Show 20 agents"
"Show next"
"Show more"
"Show previous"
"Go to page 2"

COMBINED FILTERS + PAGINATION
------------------------------
"Show first 5 agents from India"
"Show 10 inactive agents"
"Show next 20 logins"

================================================================================
                          INTENTS REFERENCE
================================================================================

INTENT NAME                  | DESCRIPTION                    | PAGINATION
-----------------------------|--------------------------------|------------
get_agent_details            | Single agent by email/ID/name  | No
get_agent_company            | Agent's company name           | No
get_agents_by_nationality    | Filter by country              | Yes (10)
get_agents_by_company        | Filter by company name         | Yes (10)
get_agent_last_login         | Last login for specific agent  | No
get_total_agents             | Count all agents               | No
get_agents_by_date           | Agents created in date range   | Yes (10)
get_recent_logins            | Latest login records           | Yes (10)
get_logins_by_date           | Logins in date range           | Yes (10)
get_inactive_agents          | Agents not logged in X days    | No
get_all_companies            | List all unique companies      | No
get_oldest_company           | Company established first      | No
get_newest_company           | Most recently established      | No
get_agents_by_email_domain   | Filter by email domain         | Yes (10)
search_agent                 | Search across all fields       | Yes (10)

================================================================================
                          KEY FEATURES
================================================================================

✅ PAGINATION
- Default 10 results per page
- Click buttons OR type commands
- Session-based (no re-query needed)
- Shows "Showing X-Y of Z results"
- Prev/Next buttons with disabled states

✅ FILTERS
- By country/nationality
- By company name
- By email domain (gmail, yahoo, etc.)
- By date ranges
- By login activity
- Combined filters

✅ SMART INTENT DETECTION
- Handles typos and variations
- Extracts parameters automatically
- Temperature 0.1 for accuracy
- Regex cleanup for intent names

✅ FAST PERFORMANCE
- First query: 2-3 seconds (Ollama)
- Pagination: < 100ms (no Ollama)
- MongoDB queries: < 50ms
- Total count: ~30ms extra

✅ PRIVACY & COST
- 100% local (no cloud)
- No data sent externally
- No API costs
- Your data stays on your machine

================================================================================
                          TROUBLESHOOTING
================================================================================

OLLAMA NOT RESPONDING
---------------------
ollama list
ollama serve
# Restart Ollama service

MONGODB CONNECTION ERROR
------------------------
mongosh
use testdb
show collections
# Verify database and collections exist

BACKEND ERRORS
--------------
# Check terminal logs
# Verify .env values
# Validate intents.json is valid JSON
# Restart: npm start

INTENT NOT DETECTED
-------------------
# Check console: "🎯 Detected intent: ..."
# If wrong intent, add more examples to intents.json
# Restart backend to reload intents

NO RESULTS RETURNED
-------------------
# Check console: "💾 DB Result: ..."
# Verify MongoDB has data
# Check query parameters: "📝 Parameters: ..."

PAGINATION NOT WORKING
----------------------
# Ensure intent has "pagination": true
# Check session storage in backend logs
# Verify frontend passes pagination prop

================================================================================
                          ADDING NEW INTENTS
================================================================================

STEP 1: Edit backend/intents/intents.json
------------------------------------------
{
  "intent": "your_new_intent",
  "collection": "agents",
  "query": { "field": "value" },
  "pagination": true,           // Optional
  "defaultLimit": 10,            // Optional
  "sort": { "field": -1 },       // Optional
  "template": "Found {count} results."
}

STEP 2: Restart backend
-----------------------
npm start

STEP 3: Test
------------
Ask a question that matches your new intent

TEMPLATE VARIABLES:
- {param} - Extracted parameter
- {count} - Result count
- {field} - Any field from result object
- {from}, {to}, {total} - Pagination metadata

================================================================================
                          API REFERENCE
================================================================================

ENDPOINT: POST /api/chat
------------------------
Request:
{
  "message": "Show agents from India",
  "sessionId": "optional-session-id"
}

Response:
{
  "reply": "Showing 1-10 of 47 results.",
  "data": [
    { "Name": "John", "UserName": "john@example.com", ... },
    ...
  ],
  "pagination": {
    "page": 1,
    "limit": 10,
    "total": 47,
    "totalPages": 5,
    "from": 1,
    "to": 10
  }
}

================================================================================
                          PERFORMANCE METRICS
================================================================================

QUERY TYPE              | TIME      | NOTES
------------------------|-----------|--------------------------------------
First query (Ollama)    | 2-3s      | Intent detection + DB query
Pagination (next/prev)  | < 100ms   | No Ollama, uses session
MongoDB find()          | < 50ms    | With indexes
MongoDB count()         | ~30ms     | For pagination total
Simple count query      | < 20ms    | No data retrieval

OPTIMIZATION TIPS:
- Add indexes on frequently queried fields
- Use pagination for large result sets
- Session storage avoids re-querying Ollama
- Limit Ollama tokens (num_predict: 50)

================================================================================
                          SECURITY NOTES
================================================================================

✅ All data stays local
✅ No external API calls (except local Ollama)
✅ No credentials stored in code
✅ MongoDB connection string in .env
✅ CORS configured for localhost only
✅ Input sanitization via MongoDB query builder

PRODUCTION RECOMMENDATIONS:
- Add authentication/authorization
- Use environment-specific .env files
- Enable MongoDB authentication
- Add rate limiting
- Implement request validation
- Use HTTPS
- Add logging and monitoring

================================================================================
                          CUSTOMIZATION
================================================================================

CHANGE UI COLORS
----------------
Edit frontend/src/styles/chat.css
- Primary color: #667eea
- Hover color: #5568d3
- Background: #f5f7fa

CHANGE DEFAULT PAGE SIZE
-------------------------
Edit backend/intents/intents.json
"defaultLimit": 20  // Change from 10 to 20

CHANGE AI MODEL
---------------
Edit backend/.env
OLLAMA_MODEL=llama2:7b  // Or any other Ollama model

ADD MORE COLLECTIONS
--------------------
1. Add to MongoDB
2. Create intents in intents.json
3. Restart backend

================================================================================
                          DEPLOYMENT
================================================================================

DEVELOPMENT
-----------
Backend: npm start (port 5000)
Frontend: npm run dev (port 3000)

PRODUCTION
----------
Backend:
  npm start (use PM2 for process management)
  
Frontend:
  npm run build
  Serve dist/ folder with nginx/apache

DOCKER (Optional)
-----------------
# Create Dockerfile for backend
# Create Dockerfile for frontend
# Use docker-compose for MongoDB + Backend + Frontend

================================================================================
                          FUTURE ENHANCEMENTS
================================================================================

POTENTIAL FEATURES:
- Export results to CSV/Excel
- Advanced filters (date pickers, multi-select)
- Charts and visualizations
- Email notifications for inactive agents
- Bulk operations
- User authentication
- Multi-language support
- Voice input
- Mobile app
- Real-time updates via WebSocket

================================================================================
                          LICENSE & CREDITS
================================================================================

License: MIT - Free to use and modify

Technologies:
- React (Meta)
- Node.js (OpenJS Foundation)
- MongoDB (MongoDB Inc.)
- Ollama (Ollama)
- Mistral 7B (Mistral AI)

================================================================================
                          SUPPORT & CONTACT
================================================================================

For issues or questions:
1. Check troubleshooting section above
2. Review console logs (backend + frontend)
3. Verify MongoDB data structure
4. Test Ollama separately: ollama run mistral:7b

Common Issues:
- Ollama not running → ollama serve
- MongoDB not connected → mongod
- Wrong intent detected → Add examples to intents.json
- Slow responses → Check Ollama performance

================================================================================
                          VERSION HISTORY
================================================================================

v1.0 - Initial Release
- Basic intent detection
- MongoDB integration
- React frontend

v1.1 - Pagination & Filters
- Added pagination support
- Session management
- Prev/Next buttons
- Result count display

v1.2 - Enhanced Intents
- Added company establishment queries
- Email domain search
- Improved parameter extraction
- Better error handling

================================================================================
                          END OF DOCUMENTATION
================================================================================
